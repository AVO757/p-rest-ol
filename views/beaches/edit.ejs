<!DOCTYPE html>
<html>

<head>
    <title>Project-REST</title>
    <link rel="stylesheet" href="/stylesheets/bootstrap.min.css">
    <link rel="stylesheet" href="/stylesheets/form.css">
    <link href="https://fonts.googleapis.com/css?family=Rancho" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Roboto:300" rel="stylesheet">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.0.13/css/all.css" integrity="sha384-DNOHZ68U8hZfKXOrtjWvjxusGo9WQnrNx2sqG0tfsghAvtVlRW3tvkXWZh58N9jp"
        crossorigin="anonymous">
</head>

<body>

    <div>
        <nav class="navbar navbar-expand-sm navbar-light bg-light">
            <a class="navbar-brand" href="/">Project REST
                <i class="fas fa-anchor"></i>
            </a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/beaches">Beaches</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/about/Project-REST">About</a>
                    </li>
                </ul>
                <ul class="navbar-nav ml-auto">
                    <% if(!currentUser) {%>
                        <li class="nav-item">
                            <a class="nav-link" href="/login">Login</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/register">Sign Up</a>
                        </li>
                        <% } else { %>
                            <li class="nav-item">
                                <a class="nav-link" href="#" onclick="return false">Signed in as
                                    <%= currentUser.username %>
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="/logout">Logout</a>
                            </li>
                            <% } %>
                </ul>
            </div>
        </nav>
    </div>

    <!-- flash messages -->
    <div class="container">
        <% if(error && error.length > 0) {%>
            <div class="alert alert-warning" role="alert">
                <%=error%>
            </div>
            <% } %>
                <% if(success && success.length > 0) {%>
                    <div class="alert alert-success" role="alert">
                        <%=success%>
                    </div>
                    <% } %>
    </div>

    <div class="grid">
        <div class="content">
            <div class="card-body container form">
                <h2 class="card-title">
                    <%=beach.name%>
                </h2>
                <hr>
                <form action="/beaches/<%=beach._id%>?_method=PUT" method="POST" enctype="multipart/form-data">
                    <div class="form-group">
                        <label>Beach name</label>
                        <input class="form-control inputText" type="text" name="beach[name]" placeholder="Name" value="<%=beach.name%>">
                    </div>
                    <div class="form-group">
                        <!-- <label>Image URL</label>
                        <input class="form-control inputText" type="text" name="beach[image]" name="image" placeholder="Image URL" value="<%=beach.image%>"> -->
                        <label for="image">Image</label>
                        <input type="file" id="image" name="beach[image]" accept="image/*">
                    </div>
                    <div class="form-group">
                        <label>Location</label>
                        <input class="form-control inputText" type="text" name="beach[location]" id="location" placeholder="Location" value="<%=beach.location%>">
                    </div>
                    <div class="form-group">
                        <label>About the beach</label>
                        <textarea class="form-control inputText" name="beach[description]" id="description" placeholder="Description"><%=beach.description%></textarea>
                    </div>
                    <input class="btn btn-primary float-right" type="submit">
                </form>
            </div>
            <div class="container option">
                <a class="btn btn-success float-right" href="/beaches">View all beaches</a>
                <form action="/beaches/<%=beach._id%>?_method=DELETE" method="POST">
                    <input class="btn btn-outline-danger" type="submit" value="Delete">
                </form>
            </div>
        </div>
        <footer>
            <div class="container contact">
                <p id="email">vo.c.alexander@gmail.com</p>
                <p id="created">Created by Alexander Van Oers</p>
                <a id="linkedIn" href="https://www.linkedin.com/in/alexander-van-oers-3284b3124/">LinkedIn</a>
                <p id="location">London, UK</p>
            </div>
        </footer>
    </div>
    

<%include ../partials/footer %>